<h1>Ingredient Index Page</h1>

<h3>Ingredients:</h3>
<ul><% @ingredients.each do |ingredient| %>
    <li><%= link_to(ingredient.name, ingredient_path(ingredient))%> - <%= pluralize(ingredient.allergic_user_count, 'Allergic User') %></li>
    <% end %>
</ul>

<h3>New Ingredient:</h3>
<%= button_to("New Ingredient", new_ingredient_path, method: :get) %>

